def is_rotation(str1, str2):
    if len(str1) != len(str2):
        return False
    # Double the sequence string
    combined = str1 + str1
    return str2 in combined

# Test execution
original = "ABCD"
check_candidate = "CDAB"
print(f"Is rotation?: {is_rotation(original, check_candidate)}")
